/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com',"framerusercontent.com"], // Add the domain here
      },
};

export default nextConfig;
